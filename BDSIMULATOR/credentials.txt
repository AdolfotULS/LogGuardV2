============================================================
 BD SIMULATOR - CREDENCIALES POSTGRESQL
============================================================

HOST        : localhost
PORT        : 5432
DATABASE    : appdb
USER        : appuser
PASSWORD    : S3cur3P@ss2024!

CONNECTION STRING:
  postgresql://appuser:S3cur3P@ss2024!@localhost:5432/appdb

PSQL CLI:
  psql -h localhost -p 5432 -U appuser -d appdb

USUARIOS EN LA BD:
  admin   / (hash en tabla users) / role: admin
  alice   / (hash en tabla users) / role: user
  bob     / (hash en tabla users) / role: user
  charlie / (hash en tabla users) / role: manager

LOGS PostgreSQL:
  Dentro del contenedor : /var/log/postgresql/
  En tu maquina        : ./logs/

============================================================
